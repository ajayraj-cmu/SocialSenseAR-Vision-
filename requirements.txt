# SocialSenseAR Vision Server
# Install: pip install -r requirements.txt
#
# IMPORTANT: torch must be installed separately with CUDA support:
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#
# HuggingFace login (needed for gated models like facebook/sam3):
#   python -c "from huggingface_hub import login; login(token='hf_YOUR_TOKEN_HERE')"

# Core
websockets>=12.0
protobuf>=4.25.0
python-dotenv>=1.0.0
numpy>=1.24.0,<3.0.0
opencv-python>=4.8.0

# ML / Vision — SAM3 requires transformers>=5.0.0
transformers>=5.0.0
huggingface-hub>=1.3.0,<2.0.0
tokenizers>=0.22.0
safetensors>=0.4.3
Pillow>=10.0.0
scikit-learn>=1.5.0

# Gemini Vision
google-genai>=1.0.0

# Emotion detection
fer>=22.5.0
tensorflow>=2.12.0

# Audio / Voice Agent
faster-whisper>=1.0.0
openai>=1.0.0
webrtcvad>=2.0.10

# PersonaPlex (optional — speech-to-speech via NVIDIA PersonaPlex-7B)
sphn>=0.1.4,<0.2
aiohttp>=3.9.0

# MediaPipe (body detection)
# GitHub: https://github.com/google-ai-edge/mediapipe
mediapipe>=0.10.0

# ── CV Model Generalization (Phase 1-3) ──────────────────────────────────────
# Install only the models you need — each is optional.

# YOLO (detection, segmentation, pose)
# GitHub: https://github.com/ultralytics/ultralytics
# Install: pip install ultralytics
# ultralytics>=8.0.0

# RF-DETR (real-time transformer detection)
# GitHub: https://github.com/roboflow/rf-detr
# Install: pip install rfdetr
# rfdetr>=0.1.0

# Depth Anything V2 (monocular depth estimation)
# GitHub: https://github.com/LiheYoung/Depth-Anything-V2
# HuggingFace: depth-anything/Depth-Anything-V2-Small-hf
# Uses 'transformers' pipeline (already required above)
# timm>=0.9.0  # needed for some Depth Anything variants

# Sapiens (Meta — pose, segmentation, depth)
# GitHub: https://github.com/facebookresearch/sapiens
# HuggingFace: facebook/sapiens-pose-0.3b
# Uses 'transformers' AutoModel (already required above)
# License: Apache 2.0 — check https://github.com/facebookresearch/sapiens/blob/main/LICENSE

# PyYAML (for reading config/cv_models.yaml)
PyYAML>=6.0
